<Project>
  <PropertyGroup>
    <Authors>AlienDomain</Authors>
    <Company>AlienDomain</Company>
    <RepositoryUrl>https://github.com/aliendomain/workable</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>work;workflow;background-jobs;recurrence;queueing;dotnet</PackageTags>
    <PackageIcon>workable-favicon.png</PackageIcon>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <Version>0.1.0</Version>
    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true'">true</ContinuousIntegrationBuild>
  </PropertyGroup>

  <PropertyGroup Condition="'$(PackageReleaseNotesFile)' != '' and Exists('$(PackageReleaseNotesFile)')">
    <PackageReleaseNotes>$([System.IO.File]::ReadAllText('$(PackageReleaseNotesFile)'))</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup Condition="'$(IsPackable)' != 'false'">
    <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" Visible="false" />
    <None Include="$(MSBuildThisFileDirectory)apps\web\workable-admin-ui\public\workable-favicon.png" Pack="true" PackagePath="\" Visible="false" />
  </ItemGroup>
</Project>
