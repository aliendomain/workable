<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PackageId>Workable.Views</PackageId>
    <Description>Shared Workable component-view contracts and query view composition used by HTTP API and SignalR adapters to project catalogs, workers, iterations, diagnostics, and overview data.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Workable.Abstractions\Workable.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Workable.Tests" />
  </ItemGroup>

</Project>
